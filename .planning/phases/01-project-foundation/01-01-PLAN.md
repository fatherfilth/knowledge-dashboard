---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - prettier.config.js
  - .gitignore
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - tailwind.config.ts
  - .env.local
autonomous: true

must_haves:
  truths:
    - "Running `npm run dev` starts the site on localhost:3000"
    - "The landing page shows 'Ryder.AI' with a styled, minimal modern design (not boilerplate)"
    - "Running `npm run build` completes without errors and produces static output"
    - "ESLint and Prettier are configured and `npm run lint` passes"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter font and metadata"
      contains: "Ryder.AI"
    - path: "src/app/page.tsx"
      provides: "Styled landing page with Ryder.AI branding"
      contains: "Ryder.AI"
      min_lines: 20
    - path: "eslint.config.mjs"
      provides: "ESLint configuration with Next.js and Prettier"
      contains: "prettier"
    - path: "prettier.config.js"
      provides: "Prettier configuration with Tailwind plugin"
      contains: "prettier-plugin-tailwindcss"
    - path: "tailwind.config.ts"
      provides: "Tailwind configuration with Inter font family"
      contains: "font-inter"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Inter font import"
      pattern: "import.*Inter.*next/font/google"
    - from: "src/app/page.tsx"
      to: "Tailwind classes"
      via: "className attributes"
      pattern: "className="
---

<objective>
Initialize the Next.js 15 project with full tooling (TypeScript, Tailwind, ESLint, Prettier) and create a styled Ryder.AI landing page with a minimal, modern aesthetic inspired by Linear and Raycast.

Purpose: Establishes the complete development foundation -- project structure, build pipeline, code quality tooling, and a designed landing page that proves the stack works end-to-end.
Output: A working Next.js 15 project that builds, runs locally, and displays a styled Ryder.AI landing page.
</objective>

<execution_context>
@C:/Users/Karl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Karl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project and configure code quality tooling</name>
  <files>
    package.json
    package-lock.json
    tsconfig.json
    next.config.ts
    eslint.config.mjs
    prettier.config.js
    .gitignore
    src/app/globals.css
    .env.local
  </files>
  <action>
    1. Run `npx create-next-app@latest . --yes` from the project root to scaffold Next.js 15 with TypeScript, Tailwind CSS, ESLint, App Router, src/ directory, and Turbopack. Note: The project directory already has files (.planning/, .claude/) so use `.` as the target and allow overwrite of any conflicting files. If create-next-app doesn't allow `.` in a non-empty directory, create in a temp directory and move files over.

    2. Install Prettier and supporting packages:
       ```
       npm install -D prettier eslint-config-prettier prettier-plugin-tailwindcss
       ```

    3. Create `prettier.config.js` at project root:
       ```javascript
       module.exports = {
         plugins: ['prettier-plugin-tailwindcss'],
         semi: true,
         singleQuote: true,
         tabWidth: 2,
         trailingComma: 'all',
         printWidth: 80,
         arrowParens: 'avoid',
       };
       ```

    4. Update `eslint.config.mjs` to include Prettier compatibility. The exact format depends on what create-next-app generates (Next.js 15 uses flat config). Add `eslint-config-prettier` to disable formatting rules that conflict with Prettier. Reference the research example but adapt to whatever create-next-app actually generates -- the key requirement is that eslint-config-prettier is included.

    5. Ensure `.gitignore` includes: `.next/`, `node_modules/`, `.env*.local`, `.env`, `out/`, `build/`, `.vercel`, `*.tsbuildinfo`, `next-env.d.ts`, `.DS_Store`. Keep any entries create-next-app generates and add any missing ones from this list.

    6. Create `.env.local` with a placeholder comment for the GITHUB_TOKEN (ready for Phase 2):
       ```
       # GitHub API token for fetching content from fatherfilth/AI-Documentation-Library
       # Uncomment and set for Phase 2:
       # GITHUB_TOKEN=your_github_token_here
       ```

    7. Verify the project scaffolded correctly: `npm run dev` should start without errors. Kill the dev server after confirming.

    IMPORTANT: Do NOT modify the `.planning/` directory or `.claude/` directory. These are project management files.
  </action>
  <verify>
    - `npm run lint` completes without errors
    - `npx prettier --check "src/**/*.{ts,tsx,css}"` passes (or shows only expected formatting)
    - `npm run build` completes without errors
    - `package.json` contains next, react, react-dom, typescript, tailwindcss, prettier, eslint-config-prettier, prettier-plugin-tailwindcss
    - `.env.local` exists with GITHUB_TOKEN placeholder comment
  </verify>
  <done>
    Next.js 15 project exists with TypeScript, Tailwind CSS, ESLint + Prettier configured and passing. Build succeeds. .env.local has GITHUB_TOKEN placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create styled Ryder.AI landing page with root layout</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    tailwind.config.ts
  </files>
  <action>
    1. Update `tailwind.config.ts` to extend the theme with Inter font family:
       ```typescript
       theme: {
         extend: {
           fontFamily: {
             sans: ['var(--font-inter)', 'system-ui', 'sans-serif'],
           },
         },
       },
       ```
       Keep the content paths that create-next-app generated.

    2. Update `src/app/globals.css` to contain only the Tailwind directives:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```
       Remove any default styles that create-next-app added (grid backgrounds, etc.).

    3. Update `src/app/layout.tsx`:
       - Import Inter from `next/font/google` with subsets: ['latin'] and variable: '--font-inter'
       - Set metadata: title = 'Ryder.AI', description = a brief tagline about AI knowledge curation (Claude's discretion on exact copy)
       - Apply Inter font variable to `<html>` element
       - Apply `font-sans antialiased` to `<body>`
       - Use `bg-white text-gray-900` for base styling

    4. Create `src/app/page.tsx` with a designed landing page:
       - This is a Server Component (no "use client")
       - Site name: "Ryder.AI" (locked decision -- use this exact name)
       - Design direction: Linear/Raycast minimal modern aesthetic (locked decision)
       - Lots of whitespace, sharp typography, no clutter
       - The page should feel DESIGNED, not boilerplate
       - Include: hero with the site name in large bold tracking-tight text, a tagline/description below in muted color, a subtle visual separator, and a footer note
       - Use Tailwind utilities for all styling: min-h-screen, flex centering, generous padding, text size hierarchy (text-6xl or text-7xl for name, text-xl or text-2xl for tagline)
       - Max width constraint (max-w-4xl or max-w-5xl) for readable width
       - Color palette: gray-900 for headings, gray-500/gray-600 for body text, gray-200 for separators, white background
       - Claude's discretion on: tagline copy, exact spacing values, whether to add subtle decorative elements (gradient text, a small icon, animated cursor blink on the name, etc.) -- as long as it stays minimal and modern

    5. Run `npm run build` to verify static output generation succeeds.

    6. Format all modified files: `npx prettier --write "src/**/*.{ts,tsx,css}" tailwind.config.ts`
  </action>
  <verify>
    - `npm run build` completes without errors
    - `npm run dev` shows the Ryder.AI landing page at http://localhost:3000 (start and visually confirm the page renders, then kill)
    - `npm run lint` passes
    - `src/app/page.tsx` contains "Ryder.AI" (exact site name)
    - `src/app/layout.tsx` imports Inter font
    - No "use client" directive in page.tsx or layout.tsx
  </verify>
  <done>
    Styled Ryder.AI landing page renders with minimal modern design (Inter font, generous whitespace, sharp typography, gray color palette). Root layout configured with font optimization and metadata. Build produces static output.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts successfully on localhost:3000
2. Landing page displays "Ryder.AI" with styled, modern design
3. `npm run build` completes without errors
4. `npm run lint` passes
5. `npx prettier --check "src/**/*.{ts,tsx,css}"` passes
6. All package versions are compatible (no peer dependency warnings)
7. .env.local exists with GITHUB_TOKEN placeholder
</verification>

<success_criteria>
- Next.js 15 project with TypeScript, Tailwind CSS, App Router, and Turbopack is initialized and running
- ESLint + Prettier are configured with Tailwind class sorting and no conflicts
- Styled Ryder.AI landing page with minimal modern aesthetic renders correctly
- Build pipeline produces static output without errors
- GITHUB_TOKEN placeholder is set up for Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
